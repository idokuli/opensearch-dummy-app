# dummy-app-http uses only the Python standard library — nothing to install.
# Kept as an explicit empty-ish file so the CI lint/build jobs have a
# consistent "pip install -r requirements.txt" step across every app.
