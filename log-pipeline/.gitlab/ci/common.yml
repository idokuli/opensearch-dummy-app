---
# =============================================================================
# Reusable job templates — shared across all pipeline jobs.
# All project-specific variables are set in GitLab Settings → CI/CD → Variables.
# =============================================================================

.base:
  interruptible: true
  tags:
    - $SHARED_RUNNERS_TAG

.docker_base:
  extends: .base
  tags:
    - $SHARED_RUNNERS_TAG
  image:
    name: $KANIKO_IMAGE
    entrypoint: [""]
